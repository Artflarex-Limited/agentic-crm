apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    app: agentic-crm
    component: frontend
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: agentic-crm
    component: frontend