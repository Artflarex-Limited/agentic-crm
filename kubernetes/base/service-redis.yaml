apiVersion: v1
kind: Service
metadata:
  name: redis
  labels:
    app: agentic-crm
    component: redis
spec:
  type: ClusterIP
  ports:
    - port: 6379
      targetPort: 6379
      protocol: TCP
      name: tcp
  selector:
    app: agentic-crm
    component: redis