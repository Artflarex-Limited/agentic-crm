apiVersion: v1
kind: Service
metadata:
  name: postgres
  labels:
    app: agentic-crm
    component: postgres
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
      name: tcp
  selector:
    app: agentic-crm
    component: postgres