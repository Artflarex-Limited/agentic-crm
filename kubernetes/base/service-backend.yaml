apiVersion: v1
kind: Service
metadata:
  name: backend
  labels:
    app: agentic-crm
    component: backend
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: agentic-crm
    component: backend