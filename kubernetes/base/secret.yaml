apiVersion: v1
kind: Secret
metadata:
  name: agentic-crm-secret
  namespace: default
type: Opaque
stringData:
  # POSTGRES_PASSWORD: "REPLACE_WITH_ACTUAL_PASSWORD"
  # SECRET_KEY: "REPLACE_WITH_ACTUAL_SECRET_KEY_MIN_32_CHARS"
  # SMTP_PASS: "REPLACE_WITH_ACTUAL_SMTP_PASSWORD"
  # LINKEDIN_COOKIES: "REPLACE_WITH_BASE64_ENCODED_COOKIES"
  # APOLLO_API_KEY: "REPLACE_WITH_ACTUAL_APOLLO_KEY"
  # TWILIO_ACCOUNT_SID: "REPLACE_WITH_ACTUAL_SID"
  # TWILIO_AUTH_TOKEN: "REPLACE_WITH_ACTUAL_TOKEN"