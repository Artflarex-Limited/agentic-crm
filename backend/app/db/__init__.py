"""
Stub module — SQLAlchemy database.py was removed.
Application uses Prisma via app.prisma instead.
"""
# This file exists to prevent import errors in code that
# imports from app.db.database (which no longer exists).
# All DB operations should use: from app.prisma import prisma